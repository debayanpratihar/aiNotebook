package com.debayan.ainotebook.common.theme

import androidx.compose.material3.Typography

/**
 * App typography. Uses the Material 3 type scale defaults for Phase 1; a custom handwriting-adjacent
 * display font can be layered in later without touching call sites (they reference
 * `MaterialTheme.typography`).
 */
val AppTypography = Typography()
