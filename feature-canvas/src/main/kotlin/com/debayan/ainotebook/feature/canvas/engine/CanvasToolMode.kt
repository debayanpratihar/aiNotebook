package com.debayan.ainotebook.feature.canvas.engine

/**
 * What a drawing gesture does. Phase 2 supports freehand drawing and stroke erasing; lasso,
 * selection, and shape tools are added in later drawing-tool work.
 */
enum class CanvasToolMode {
    DRAW,
    STROKE_ERASER,
}
